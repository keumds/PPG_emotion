// ROOT build.gradle (plugins DSL 방식, 최신 구조)

plugins {
    id 'com.android.application' version '8.13.0' apply false
    id 'com.android.library'     version '8.13.0' apply false
    // Kotlin을 사용 중이라면:
    // id 'org.jetbrains.kotlin.android' version '2.1.20' apply false
}
